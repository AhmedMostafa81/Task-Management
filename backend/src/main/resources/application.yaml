spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/task_management_db
    username: ahmed
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create